<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
    <head>
        <title><%= content_for(:title) || "Devise" %></title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= javascript_importmap_tags %>
    </head>
    <body class="bg-custom-gradient bg-cover text-dark font-sans flex flex-col min-h-screen !important">
        <div class="container mx-auto p-4 sm:px-6 md:px-8">
            <% if flash[:notice] %>
                <p class="flash-message notice"><%= flash[:notice] %></p>
            <% end %>
            <% if flash[:alert] %>
                <p class="flash-message alert"><%= flash[:alert] %></p>
            <% end %>
            <%= yield %>
        </div>
    </body>
</html>
