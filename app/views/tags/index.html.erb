<div data-controller="tags" class="max-w-6xl mx-auto relative">
    <nav class="flex flex-wrap gap-2 mb-6 shadow-md bg-gray-100 p-2 rounded-lg">
        <% @tags.each do |tag| %>
            <button data-action="click->tags#load" data-id="<%= tag.id %>" data-tags-target="tab" class="px-4 py-2 rounded-md text-sm font-medium transition-colors duration-150 focus:outline-none focus:ring-2 focus:shadow-lg focus:ring-offset-2 focus:ring-primary-500 bg-white hover:bg-primary-300">
                <%= tag.name %>
            </button>
        <% end %>
    </nav>

    <div data-tags-target="loading" class="absolute inset-0 flex items-center justify-center bg-gray-200/75 hidden">
        <div class="bg-white p-4 flex flex-col items-center justify-center gap-5 rounded-lg shadow-md  top-[50%] left-[50%]">
            <plex class="text-center text-gray-700">Makaleler yükleniyor...</plex>
            <div class="animate-spin rounded-full h-8 w-8 border-t-2 border-b-2 border-primary-500"></div>
        </div>
    </div>

    <div data-tags-target="error" class="hidden absolute inset-0 flex items-center justify-center bg-red-200/75">
        <div class="bg-white p-4 rounded-lg shadow-md">
            <p class="text-center text-red-700">Makaleler yüklenirken bir hata oluştu.</p>
        </div>
    </div>

    <div data-tags-target="content">
        <p class="text-center text-gray-500">Bir etikete tıklayarak o etikete ait makaleleri görüntüleyebilirsiniz.</p>
    </div>
</div>
